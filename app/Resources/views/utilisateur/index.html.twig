{% extends 'base.html.twig' %}

{% block content %}
<ul class="breadcrumb">
  <li>
    <a href="#">Home</a>
  </li>

</ul>
<!-- END BREADCRUMB -->
<div class="row">
  <div class="col-md-12">

    <div class="page-title">
  <h3><span class="fa fa-users"></span> Users </h3>
  </div>
<div class="panel panel-default">
  <div class="panel-body">

    <!-- START RESPONSIVE TABLES -->
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
 <h3> <center><br/>Administration des utilisateurs</center></h3></br> </br>
            <div class="panel-heading">
                <h3 class="panel-title"> Liste des Utilisateurs</h3>
            </div>

            <div class="panel-body panel-body-table">

                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-actions">
                        <thead>
                            <tr>

                                <th>Nom</th>
                                <th >Prénom</th>
                                <th >Sexe</th>
                                <th >Age</th>
                                <th>Email</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                          {% for utilisateur in utilisateurs %}
                              <tr>

                                  <td><strong>{{ utilisateur.nom }}</strong></td>
                                  <td>{{ utilisateur.prenom }}</td>
                                  <td>{{ utilisateur.sexe }}</td>
                                  <td> {{ utilisateur.age }}</td>
                                  <td> {{ utilisateur.email }}</td>
                                  <td>
                                    <a class="btn btn-default btn-rounded btn-condensed " href="{{ path('utilisateur_edit', { 'id': utilisateur.id }) }}"><span class="fa fa-pencil">Edit</span></a>
                                    <a class="btn btn-info btn-rounded btn-condensed "   href="{{ path('utilisateur_show', { 'id': utilisateur.id }) }}" ><span class="fa fa-eye">Show</span></a>
                                    <a class="btn btn-danger btn-rounded btn-condensed"  href="{{path('utilisateur_edit', { 'id': utilisateur.id })}}"><span class="fa fa-trash"></span>Delete</a>

                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>

            </div>
        </div>

    </div>
</div>


{% endblock %}
